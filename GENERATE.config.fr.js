module.exports = {
	// You may add files like: 'initPostgreSql.js', 'models', etc inside the refersh property array to hard reload your server when these files get changed
	refresh: [],
	// You can have values like '', '--inspect', '--inspect-brk'.
	debug: '--inspect',
}
